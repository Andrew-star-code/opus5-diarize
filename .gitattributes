# Нормализуем переводы строк: проект собирается в Linux-контейнерах,
# а редактируется в Windows. Без этого каждый файл, тронутый на другой
# системе, выглядит изменённым целиком.
* text=auto eol=lf
*.ps1 text eol=crlf
*.png binary
*.woff2 binary
