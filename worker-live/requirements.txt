# Ядро стримингового распознавания. Берём как библиотеку:
# свой UI, своё хранилище, свой протокол WebSocket.
#
# Версия закреплена жёстко: engine.py подменяет его внутренности
# (выравнивание слов, фабрику диаризации), и новая версия может молча
# сломать подмены. Обновлять — вместе с проверками из tests/.
whisperlivekit==0.2.26

# Живая диаризация — NVIDIA Streaming Sortformer. На замере
# (bench/eval_live.py) он путает говорящих втрое реже diart и находит
# третьего собеседника, которого diart терял. Версия — та же ветка NeMo,
# что закреплена у WhisperLiveKit для Sortformer.
#
# diart здесь больше нет: он держит numpy ниже 2, а NeMo 3 требует 2.
nemo_toolkit[asr]==3.0.0

ctranslate2>=4.6
faster-whisper>=1.1

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
websockets>=12.0
httpx>=0.27,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.4,<3.0
numpy>=2,<3
