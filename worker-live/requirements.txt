# Ядро стримингового распознавания. Берём как библиотеку:
# свой UI, своё хранилище, свой протокол WebSocket.
whisperlivekit>=0.2

# Онлайн-диаризация. diart считает на эмбеддингах pyannote и не привязан
# к языку — в отличие от streaming Sortformer от NVIDIA, обученного
# преимущественно на английском.
diart>=0.9

# diart 0.9.2 зовёт matplotlib.cm.get_cmap, а его удалили в matplotlib 3.9.
# Без этого пина импорт diart падает, и живая диаризация не стартует вовсе.
matplotlib<3.9

# pyannote 3.x, который тянет diart, зовёт hf_hub_download с аргументом
# use_auth_token. В huggingface_hub 1.0 его удалили, и загрузка моделей
# падает на TypeError.
huggingface_hub<1.0

ctranslate2>=4.6
faster-whisper>=1.1

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
websockets>=12.0
httpx>=0.27,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.4,<3.0
numpy>=1.26,<3
